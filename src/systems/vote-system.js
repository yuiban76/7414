export function createVote({id,choices,playerIds,hostPlayerId}){if(!id||choices.length<2)throw new Error("投票至少需要兩個選項");return{id,choices:[...choices],eligiblePlayerIds:[...playerIds],hostPlayerId,ballots:{},status:"open",result:null};}
export function castVote(vote,playerId,choiceId){if(vote.status!=="open")throw new Error("投票已結束");if(!vote.eligiblePlayerIds.includes(playerId))throw new Error("玩家沒有投票資格");if(!vote.choices.includes(choiceId))throw new Error("投票選項不存在");vote.ballots[playerId]=choiceId;return vote;}
export function resolveVote(vote,hostTieChoice=null){const counts=Object.fromEntries(vote.choices.map(id=>[id,0]));for(const choice of Object.values(vote.ballots))counts[choice]++;const max=Math.max(...Object.values(counts));const leaders=vote.choices.filter(id=>counts[id]===max);if(leaders.length>1){if(!hostTieChoice||!leaders.includes(hostTieChoice))return{status:"tie",leaders,counts};vote.result=hostTieChoice;}else vote.result=leaders[0];vote.status="resolved";return{status:"resolved",result:vote.result,counts};}
